import { Collection, Snowflake } from 'discord.js';
import PartialProfile from '../../structures/PartialProfile';
declare function search(this: import('../../structures/Base'), query: string): Promise<Collection<Snowflake, PartialProfile>>;
export = search;
