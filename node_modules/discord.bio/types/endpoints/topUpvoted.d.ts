import { Bio } from '..';
import PartialProfile from '../structures/PartialProfile';
import { Collection, Snowflake } from 'discord.js';
declare function topUpvoted(this: Bio): Promise<Collection<Snowflake, PartialProfile>>;
export = topUpvoted;
